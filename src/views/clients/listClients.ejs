<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listar Clientes</title>
</head>
<body>
    <ul>
    <% for(let i = 0; i < users.length; i++) { %>
        <li>
            <p>Nome: <%= users[i].nome %></p>
            <p>Sobrenome: <%= users[i].sobrenome %></p>
            <p>Email: <%= users[i].email %></p>
            <p>CPF: <%= users[i].cpf %></p>
            <p>Telefone: <%= users[i].telefone %></p>
            <p>Data de Nascimento: <%= users[i].data_nascimento %></p>
            
            <button><a href="/clientes/editar/<%= users[i].id %>">
                Editar Usuario
            </a></button>
            <form action="/clientes/deletar/<%= users[i].id %>?_method=DELETE" method="post">
                <button type="submit">Deletar Usuario</button>
            </form>
        </li>
        <% } %>
    </ul>
    <button><a href="/clientes/cadastrar">Cadastrar novo Usuario</a></button>
</body>
</html>